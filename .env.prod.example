# ====================================
# Production Environment (Supabase)
# ====================================
# 사용법: cp production.env.example production.env 후 아래 값들을 수정하여 저장
# terraform-apply.sh가 이 파일에서 Terraform 변수를 읽음
#
# ⚠️  주의: production.env는 .gitignore에 등록되므로 git에 올라가지 않음
#          민감한 정보이므로 절대 git에 commit하면 안 됨

# ====================================
# Supabase PostgreSQL
# ====================================
# Supabase 프로젝트 설정에서 얻을 수 있음
# Connection pooling 사용 (포트 6543)
# 예시:
# DB_HOST=aws-1-ap-northeast-2.pooler.supabase.com
# DB_HOST=europe-west1.pooler.supabase.com
DB_HOST=your-project.pooler.supabase.com

# 포트는 Supabase pooler 기본 포트 (변경 금지)
DB_PORT=6543

# 데이터베이스명 (Supabase 기본값: postgres)
# 변경 시 Supabase에서 직접 DB 생성 필요
DB_NAME=postgres

# 사용자명 형식: postgres.프로젝트_ID
# 예시: postgres.abcdefghijklmnopqrst
DB_USERNAME=postgres.your_project_id

# 사용자 비밀번호 (Supabase 생성 시 설정한 비밀번호)
# 절대 git에 commit하지 말 것!
DB_PASSWORD=your_supabase_password

# ====================================
# API Keys
# ====================================
# OpenAI API 키 (sk-proj- 또는 sk-로 시작)
OPENAI_API_KEY=sk-proj-your-api-key-here

# Kakao REST API 키
KAKAO_REST_API_KEY=your_kakao_rest_api_key

# Kakao JavaScript 키
KAKAO_JAVASCRIPT_KEY=your_kakao_javascript_key

# ====================================
# 모니터링 (선택사항)
# ====================================
# CloudWatch 알람을 받을 이메일
ALARM_EMAIL=your-email@example.com
