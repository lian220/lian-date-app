# Terraform files
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
# .terraform.lock.hcl - 주석 처리: 일반적으로 버전 관리에 포함해야 함 (provider 버전 고정)

# Provider plugins (downloaded providers)
terraform.d/
.terraform.d/

# Sensitive files (may contain passwords, secrets, etc.)
*.tfvars
terraform.tfvars
!*.tfvars.example

# Crash log files
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# Plan files (may contain sensitive data)
*.tfplan
*.tfplan.json

# Temporary files
*.tmp
*.log
