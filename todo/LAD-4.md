# LAD-4: [F3] 코스 상세 보기 및 장소 정보

**티켓 URL**: https://liandy220-developer.atlassian.net/browse/LAD-4
**상위 에픽**: LAD-1 - [MVP] 데이트 코스 추천 기능 V1
**상태**: 해야 할 일

---

## 📋 User Story

> "사용자로서, 추천된 각 장소의 상세 정보를 확인하고 싶다. 실제로 방문하기 전에 장소를 파악하고 판단하기 위해서다."

## 🎯 기능 설명

- 코스 전체 지도 뷰
- 개별 장소 상세 정보 카드
- 카카오맵 연동 (길찾기)

---

## 📊 하위 작업 목록 (총 10개)

### Frontend (7개)

| ID | 제목 | 설명 | 우선순위 | 예상 소요 |
|----|------|------|----------|-----------|
| **LAD-29** | 카카오맵 지도 컴포넌트 | 카카오맵 SDK 설정 및 지도 렌더링 | ⭐⭐⭐ 높음 | 1일 |
| **LAD-30** | 장소 마커 표시 | 순서 번호가 표시된 마커 + 경로 점선 | ⭐⭐⭐ 높음 | 1일 |
| **LAD-31** | 장소 상세 바텀시트 | 장소 정보 표시 UI (슬라이드업) | ⭐⭐⭐ 높음 | 1일 |
| **LAD-32** | 주소 클립보드 복사 | Clipboard API 활용한 복사 기능 | ⭐⭐ 중간 | 0.5일 |
| **LAD-33** | 전화 연결 기능 | tel: URI로 전화 앱 연결 | ⭐⭐ 중간 | 0.5일 |
| **LAD-34** | 카카오맵 딥링크 | 카카오맵 앱/웹 연동 | ⭐⭐ 중간 | 0.5일 |
| **LAD-35** | 길찾기 버튼 | 카카오맵 길찾기 실행 + 시간 표시 | ⭐⭐ 중간 | 0.5일 |

### Backend (3개)

| ID | 제목 | 설명 | 우선순위 | 예상 소요 |
|----|------|------|----------|-----------|
| **LAD-36** | 코스 상세 조회 API | GET /courses/{id} | ⭐⭐⭐ 높음 | 1.5일 |
| **LAD-37** | 장소 상세 조회 API | GET /places/{id} | ⭐⭐⭐ 높음 | 1.5일 |
| **LAD-38** | 영업시간 파싱 로직 | 카카오맵 영업시간 문자열 파싱 | ⭐⭐ 중간 | 0.5일 |

---

## 🔄 병렬 실행 전략

### ✅ 그룹별 병렬 실행 가능 작업

```
┌─────────────────────────────────────────────────────────────┐
│ Phase 1: Backend 기반 작업 (병렬 실행)                       │
├─────────────────────────────────────────────────────────────┤
│ LAD-38 (영업시간 파싱) ──┐                                  │
│                          ├─→ 3개 동시 실행 가능              │
│ LAD-36 (코스 상세 API) ──┤                                  │
│                          │                                   │
│ LAD-37 (장소 상세 API) ──┘                                  │
│                                                              │
│ 예상 소요: 2일 (병렬 실행 시)                                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Phase 2: Frontend 핵심 UI (일부 병렬)                        │
├─────────────────────────────────────────────────────────────┤
│ LAD-29 (지도) ───→ LAD-30 (마커) [순차]                     │
│                                                              │
│ LAD-31 (바텀시트) [병렬 가능, Mock 데이터 사용]              │
│                                                              │
│ 예상 소요: 3일 (일부 병렬 실행 시)                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Phase 3: Frontend 유틸리티 (완전 병렬)                       │
├─────────────────────────────────────────────────────────────┤
│ LAD-32 (클립보드) ──┐                                       │
│                     ├─→ 4개 동시 실행 가능                  │
│ LAD-33 (전화연결) ──┤                                       │
│                     │                                        │
│ LAD-34 (딥링크)  ───┤                                       │
│                     │                                        │
│ LAD-35 (길찾기)  ───┘                                       │
│                                                              │
│ 예상 소요: 0.5일 (4개 병렬 실행 시)                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Phase 4: 통합 테스트                                         │
├─────────────────────────────────────────────────────────────┤
│ - BE API ↔ FE 연동 테스트                                   │
│ - 카카오맵 딥링크 실기기 테스트                              │
│ - 전체 시나리오 E2E 테스트                                   │
│                                                              │
│ 예상 소요: 1일                                               │
└─────────────────────────────────────────────────────────────┘
```

**총 예상 기간**: 순차 실행 시 9일 → 병렬 실행 시 **4~5일** (약 50% 단축)

---

## 📝 작업 의존성 맵

```
LAD-38 (BE 파싱)
    ↓
LAD-37 (BE 장소 API) ────→ LAD-31 (FE 바텀시트)
    ↓                           ↓
LAD-36 (BE 코스 API) ────→ LAD-29 (FE 지도)
                                ↓
                            LAD-30 (FE 마커)
                                ↓
                    ┌───────────┴───────────┐
                    ↓                       ↓
            LAD-34 (딥링크)          LAD-35 (길찾기)

LAD-32 (클립보드) ──┐
                    ├─→ 완전 독립 (언제든지 개발 가능)
LAD-33 (전화연결) ──┘
```

---

## 🚀 Frontend 작업 상세

### LAD-29: 카카오맵 지도 컴포넌트

**우선순위**: ⭐⭐⭐ 높음 (선행 작업)
**예상 소요**: 1일
**의존성**: 없음

#### 구현 내용
1. 카카오맵 JavaScript SDK 설정
2. 지도 초기화 및 렌더링
3. 중심 좌표 설정 (코스의 중간 지점)
4. 줌 레벨 조정
5. 지도 컨트롤 (줌, 이동)

#### 예상 파일
```
frontend/src/
├── components/kakao/
│   ├── KakaoMap.tsx           # 지도 컴포넌트
│   └── KakaoMapScript.tsx     # SDK 로드
└── hooks/
    └── useKakaoMap.ts         # 지도 초기화 훅
```

#### 체크리스트
- [ ] 지도가 정상적으로 렌더링됨
- [ ] 줌 인/아웃 동작
- [ ] 드래그로 지도 이동 가능
- [ ] 중심 좌표 변경 시 지도 업데이트

---

### LAD-30: 장소 마커 표시

**우선순위**: ⭐⭐⭐ 높음
**예상 소요**: 1일
**의존성**: LAD-29 완료 필수

#### 구현 내용
1. 순서 번호(1, 2, 3)가 표시된 커스텀 마커
2. 각 장소 좌표에 마커 배치
3. 마커 클릭 이벤트 처리
4. 장소 간 점선 경로 표시 (폴리라인)

#### 예상 파일
```
frontend/src/
└── components/kakao/
    ├── PlaceMarker.tsx        # 마커 컴포넌트
    └── RoutePolyline.tsx      # 경로 점선
```

#### 체크리스트
- [ ] 3개 마커가 순서대로 표시됨
- [ ] 마커에 순서 번호 표시됨
- [ ] 마커 클릭 시 이벤트 발생
- [ ] 장소 간 점선 경로 표시됨

---

### LAD-31: 장소 상세 바텀시트

**우선순위**: ⭐⭐⭐ 높음
**예상 소요**: 1일
**의존성**: LAD-37 (Mock 데이터로 먼저 개발 가능)

#### 구현 내용
1. 하단 슬라이드업 바텀시트 UI
2. 장소 정보 표시 (장소명, 카테고리, 주소, 영업시간, 전화번호, 평점, AI 추천 이유)
3. 인터랙션 (주소 복사, 전화 연결, 카카오맵 딥링크)
4. 드래그로 닫기 기능

#### 예상 파일
```
frontend/src/
├── components/
│   └── PlaceDetailSheet.tsx   # 바텀시트
└── types/
    └── place.ts               # PlaceDetail 타입
```

#### Mock 데이터 예시
```typescript
interface PlaceDetail {
  id: string;
  name: string;
  category: string;
  address: string;
  phone: string;
  businessHours: string;
  rating?: number;
  aiReason: string;
}
```

#### 체크리스트
- [ ] 바텀시트가 슬라이드업 애니메이션으로 표시됨
- [ ] 주소 클릭 시 클립보드 복사
- [ ] 전화번호 클릭 시 전화 앱 실행
- [ ] 모든 정보가 정상 표시됨
- [ ] "카카오맵에서 보기" 버튼 동작

---

### LAD-32: 주소 클립보드 복사

**우선순위**: ⭐⭐ 중간
**예상 소요**: 0.5일
**의존성**: 없음

#### 구현 내용
1. `navigator.clipboard.writeText()` API 사용
2. 복사 성공 시 토스트/알림 표시
3. 폴백 처리 (구형 브라우저)

#### 예상 파일
```
frontend/src/
└── hooks/
    └── useClipboard.ts        # 클립보드 훅
```

#### 체크리스트
- [ ] 주소 복사 성공
- [ ] 복사 성공 피드백 표시
- [ ] 구형 브라우저 폴백 동작
- [ ] 모바일 환경 테스트

---

### LAD-33: 전화 연결 기능

**우선순위**: ⭐⭐ 중간
**예상 소요**: 0.5일
**의존성**: 없음

#### 구현 내용
1. `tel:` URI Scheme 사용
2. 전화번호 포맷팅
3. 모바일/데스크톱 분기 처리

#### 예상 파일
```
frontend/src/
└── lib/
    └── phone.ts               # 전화 유틸리티
```

#### 체크리스트
- [ ] 모바일에서 전화 앱 실행
- [ ] 데스크톱에서 번호 복사
- [ ] 전화번호 포맷팅 정상 동작

---

### LAD-34: 카카오맵 딥링크

**우선순위**: ⭐⭐ 중간
**예상 소요**: 0.5일
**의존성**: 없음

#### 구현 내용
1. 카카오맵 앱 설치 여부 확인
2. 설치됨: `kakaomap://` 스킴 실행
3. 미설치: 카카오맵 웹 페이지 오픈

#### 예상 파일
```
frontend/src/
└── lib/kakao/
    └── deeplink.ts            # 딥링크 유틸리티
```

#### 체크리스트
- [ ] 앱 설치 시 카카오맵 앱 실행
- [ ] 미설치 시 웹 페이지 오픈
- [ ] 장소 상세 화면 정상 이동

---

### LAD-35: 길찾기 버튼

**우선순위**: ⭐⭐ 중간
**예상 소요**: 0.5일
**의존성**: 없음

#### 구현 내용
1. 카카오맵 길찾기 실행 (출발지/도착지)
2. 예상 이동 시간 표시 (거리 계산)
3. 이동 수단 옵션

#### 예상 파일
```
frontend/src/
└── lib/kakao/
    └── directions.ts          # 길찾기 유틸리티
```

#### 체크리스트
- [ ] 길찾기 버튼 클릭 시 카카오맵 길찾기 실행
- [ ] 출발지/도착지 정상 전달
- [ ] 예상 이동 시간 표시

---

## 🛠 Backend 작업 상세

### LAD-36: 코스 상세 조회 API (GET /courses/{id})

**우선순위**: ⭐⭐⭐ 높음
**예상 소요**: 1.5일
**의존성**: 없음

#### API 스펙

**Endpoint**: `GET /api/v1/courses/{id}`

**Response**:
```json
{
  "id": "course-uuid",
  "title": "강남 데이트 코스",
  "region": "서울 강남구",
  "dateType": "ROMANTIC",
  "budget": 150000,
  "places": [
    {
      "id": "place-uuid-1",
      "name": "이탈리안 레스토랑",
      "category": "음식점 > 이탈리안",
      "lat": 37.5665,
      "lng": 126.9780,
      "order": 1,
      "estimatedCost": 60000
    },
    {
      "id": "place-uuid-2",
      "name": "카페",
      "category": "카페",
      "lat": 37.5675,
      "lng": 126.9790,
      "order": 2,
      "estimatedCost": 20000
    },
    {
      "id": "place-uuid-3",
      "name": "영화관",
      "category": "문화시설 > 영화관",
      "lat": 37.5685,
      "lng": 126.9800,
      "order": 3,
      "estimatedCost": 30000
    }
  ],
  "totalCost": 110000,
  "aiReason": "분위기 좋은 레스토랑에서 시작해 여유로운 카페 타임을 거쳐 영화로 마무리하는 완벽한 코스입니다."
}
```

#### 구현 내용
1. `GetCourseDetailUseCase` 구현
2. 코스 정보 + 장소 리스트 조회
3. 장소 순서대로 정렬
4. 예외 처리 (코스 없음, 권한 없음 등)

#### 예상 파일
```
backend/src/main/kotlin/com/dateclick/api/
├── presentation/rest/
│   └── CourseController.kt
├── application/course/
│   └── GetCourseDetailUseCase.kt
└── domain/course/
    └── entity/CourseDetail.kt
```

#### 체크리스트
- [ ] API 엔드포인트 구현 완료
- [ ] 코스 + 장소 리스트 정상 조회
- [ ] 장소 순서 정렬 정상 동작
- [ ] Swagger 문서화
- [ ] 단위 테스트 작성

---

### LAD-37: 장소 상세 조회 API (GET /places/{id})

**우선순위**: ⭐⭐⭐ 높음
**예상 소요**: 1.5일
**의존성**: LAD-38 (영업시간 파싱)

#### API 스펙

**Endpoint**: `GET /api/v1/places/{id}`

**Response**:
```json
{
  "id": "place-uuid",
  "name": "이탈리안 레스토랑",
  "category": "음식점 > 이탈리안",
  "address": "서울시 강남구 테헤란로 123",
  "roadAddress": "서울시 강남구 역삼동 123-45",
  "phone": "02-1234-5678",
  "businessHours": "매일 11:00~22:00",
  "lat": 37.5665,
  "lng": 126.9780,
  "rating": 4.5,
  "reviewCount": 234,
  "kakaoPlaceId": "12345678",
  "aiReason": "분위기가 로맨틱하고 음식이 맛있어 데이트하기 좋은 곳입니다."
}
```

#### 구현 내용
1. `GetPlaceDetailUseCase` 구현
2. 카카오맵 API 호출 (장소 상세 정보)
3. 영업시간 파싱 (LAD-38)
4. 응답 매핑

#### 예상 파일
```
backend/src/main/kotlin/com/dateclick/api/
├── presentation/rest/
│   └── PlaceController.kt
├── application/place/
│   └── GetPlaceDetailUseCase.kt
├── domain/place/
│   ├── entity/PlaceDetail.kt
│   └── vo/BusinessHours.kt
└── infrastructure/external/kakao/
    └── KakaoPlaceClient.kt
```

#### 체크리스트
- [ ] API 엔드포인트 구현 완료
- [ ] 카카오맵 API 연동
- [ ] 영업시간 파싱 적용
- [ ] Swagger 문서화
- [ ] 단위 테스트 작성

---

### LAD-38: 영업시간 파싱 로직

**우선순위**: ⭐⭐ 중간
**예상 소요**: 0.5일
**의존성**: 없음 (유틸리티 성격)

#### 구현 내용
1. 카카오맵 영업시간 문자열 파싱
2. 다양한 포맷 대응
   - "매일 09:00~22:00"
   - "월-금 10:00-20:00, 주말 11:00-18:00"
   - "연중무휴"
   - "정보 없음"
3. 구조화된 데이터로 변환

#### 예상 파일
```
backend/src/main/kotlin/com/dateclick/api/
├── domain/place/vo/
│   └── BusinessHours.kt
└── infrastructure/util/
    └── BusinessHoursParser.kt
```

#### 파싱 예시
```kotlin
// Input: "매일 11:00~22:00"
// Output: BusinessHours(
//   type = "DAILY",
//   openTime = "11:00",
//   closeTime = "22:00",
//   dayOfWeek = null
// )

// Input: "월-금 10:00-20:00, 주말 11:00-18:00"
// Output: List<BusinessHours>(
//   BusinessHours(type = "WEEKDAY", openTime = "10:00", closeTime = "20:00"),
//   BusinessHours(type = "WEEKEND", openTime = "11:00", closeTime = "18:00")
// )
```

#### 체크리스트
- [ ] 다양한 영업시간 포맷 파싱
- [ ] 예외 케이스 처리 ("정보 없음", "연중무휴" 등)
- [ ] 단위 테스트 작성 (다양한 케이스)
- [ ] LAD-37에 통합

---

## 📝 Acceptance Criteria 전체 체크리스트

### AC 3.1: 코스 지도 표시

- [ ] 카카오맵에 3개 장소가 마커로 표시
- [ ] 마커에 순서 번호(1, 2, 3) 표시
- [ ] 장소 간 이동 경로가 점선으로 연결
- [ ] 지도 줌/이동 가능

### AC 3.2: 장소 상세 정보 확인

- [ ] 장소 카드 탭 시 상세 바텀시트 표시
- [ ] 장소명, 카테고리 정보 포함
- [ ] 주소 (탭 시 클립보드 복사) 포함
- [ ] 영업시간 정보 포함
- [ ] 전화번호 (탭 시 전화 앱 연결) 포함
- [ ] 카카오맵 평점 (있는 경우) 포함
- [ ] AI 추천 이유 (1~2문장) 포함

### AC 3.3: 카카오맵 연동

- [ ] "카카오맵에서 보기" 버튼 탭 시:
  - [ ] 앱 설치 시: 해당 장소 상세 화면으로 이동
  - [ ] 미설치 시: 카카오맵 웹 페이지 브라우저 오픈

### AC 3.4: 장소 간 길찾기

- [ ] "이동 경로" 버튼 탭 시 카카오맵 길찾기 실행 (출발지: A, 도착지: B)
- [ ] 예상 이동 시간 사전 표시 (예: "도보 15분")

---

## 📂 전체 파일 구조

### Frontend
```
frontend/src/
├── app/
│   └── courses/
│       └── [id]/
│           ├── page.tsx                 # 코스 상세 페이지
│           └── components/
│               ├── CourseMap.tsx        # LAD-29, LAD-30
│               └── RouteButton.tsx      # LAD-35
├── components/
│   ├── kakao/
│   │   ├── KakaoMap.tsx                 # LAD-29
│   │   ├── KakaoMapScript.tsx           # LAD-29
│   │   └── PlaceMarker.tsx              # LAD-30
│   └── PlaceDetailSheet.tsx             # LAD-31
├── lib/
│   ├── kakao/
│   │   ├── deeplink.ts                  # LAD-34
│   │   ├── directions.ts                # LAD-35
│   │   └── types.ts
│   └── phone.ts                         # LAD-33
├── hooks/
│   ├── useClipboard.ts                  # LAD-32
│   └── useKakaoMap.ts                   # LAD-29
└── types/
    └── place.ts                         # 공통 타입
```

### Backend
```
backend/src/main/kotlin/com/dateclick/api/
├── presentation/rest/
│   ├── CourseController.kt              # LAD-36
│   └── PlaceController.kt               # LAD-37
├── application/
│   ├── course/
│   │   └── GetCourseDetailUseCase.kt    # LAD-36
│   └── place/
│       └── GetPlaceDetailUseCase.kt     # LAD-37
├── domain/
│   ├── course/
│   │   └── entity/CourseDetail.kt       # LAD-36
│   └── place/
│       ├── entity/PlaceDetail.kt        # LAD-37
│       └── vo/BusinessHours.kt          # LAD-38
└── infrastructure/
    ├── external/kakao/
    │   └── KakaoPlaceClient.kt          # LAD-37
    └── util/
        └── BusinessHoursParser.kt       # LAD-38
```

---

## 🎯 실행 계획

### Week 1: 기반 구축

**Day 1-2: Backend 병렬 시작**
```
Developer A (BE):
  ✓ LAD-38: 영업시간 파싱 로직 (0.5일)
  ✓ LAD-36: 코스 상세 조회 API (1.5일)

Developer B (BE):
  ✓ LAD-37: 장소 상세 조회 API (1.5일, LAD-38 완료 후 통합)
```

**Day 3-4: Frontend 핵심 UI**
```
Developer C (FE):
  ✓ LAD-29: 카카오맵 지도 컴포넌트 (1일)
  ✓ LAD-30: 장소 마커 표시 (1일, LAD-29 완료 후)

Developer D (FE):
  ✓ LAD-31: 장소 상세 바텀시트 (1일, Mock 데이터 사용)
```

### Week 2: 통합 및 유틸리티

**Day 5: API 통합**
```
  ✓ LAD-31 ↔ LAD-37 연동 테스트 (0.5일)
  ✓ TypeScript 타입 정의 검증
```

**Day 6: 유틸리티 병렬 실행**
```
4개 작업 동시 진행 (각 0.5일):
  ✓ LAD-32: 주소 클립보드 복사
  ✓ LAD-33: 전화 연결 기능
  ✓ LAD-34: 카카오맵 딥링크
  ✓ LAD-35: 길찾기 버튼
```

**Day 7: 통합 테스트 & QA**
```
  ✓ 전체 기능 통합 테스트
  ✓ 카카오맵 딥링크 실기기 테스트
  ✓ 모바일 반응형 테스트
  ✓ AC 전체 항목 검증
```

---

## 🛡 기술 스택 & 도구

### Frontend
- **지도**: Kakao Maps JavaScript SDK
- **UI**: React 19 + TypeScript + Tailwind CSS 4
- **딥링크**: Kakao Map Scheme (kakaomap://)
- **클립보드**: Navigator Clipboard API
- **전화**: tel: URI scheme

### Backend
- **API**: Spring Boot 3.4.1 + Kotlin
- **외부 API**: Kakao Maps REST API
- **파싱**: 정규식 기반 영업시간 파싱

---

## 📌 참고사항

- **카카오맵 API 키**: `.env` 파일에 `KAKAO_JAVASCRIPT_KEY` 설정 필요
- **딥링크 테스트**: 실제 모바일 기기에서 테스트 권장
- **영업시간 파싱**: 다양한 포맷 대응 필요
- **에러 처리**: SDK 로드 실패, API 타임아웃 등 예외 처리 필요
- **Mock 데이터**: FE 작업 시 BE API 완료 전까지 Mock 데이터 사용

---

## ✅ 다음 단계

1. **브랜치 생성**: `feature/LAD-4-course-detail-view`
2. **Phase 1 시작**: BE 작업 3개 병렬 진행
3. **API 스펙 문서화**: Swagger 업데이트
4. **FE 개발 환경 준비**: Kakao Maps JavaScript SDK 설정
5. **Phase 2 진행**: BE API 완료 후 FE 핵심 UI 개발
6. **Phase 3 실행**: FE 유틸리티 병렬 개발
7. **통합 테스트**: E2E 테스트 및 QA
