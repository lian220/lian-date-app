# AWS 계정 설정 (로컬 개발용)
# 이 파일을 .env.aws로 복사하여 사용하세요: cp .env.aws.example .env.aws
# 주의: .env.aws 파일은 절대 Git에 커밋하지 마세요!

# AWS 계정 정보
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1

# AWS 자격 증명 (로컬 개발/테스트용)
# 프로덕션에서는 OIDC를 사용하므로 이 값들은 사용하지 않습니다
# AWS CLI 프로파일을 사용하는 것을 권장합니다: aws configure --profile lian-date
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# GitHub Actions에서는 아래 정보가 GitHub Secrets로 설정되어야 합니다:
# - AWS_ACCOUNT_ID: AWS 계정 ID (12자리 숫자)
# - AWS_REGION: us-east-1
# - AWS_ROLE_ARN: arn:aws:iam::123456789012:role/github-actions-deployment-role

# ECR Repository (추후 생성 후 업데이트)
ECR_REPOSITORY_BACKEND=
ECR_REPOSITORY_FRONTEND=

# ECS Cluster (추후 생성 후 업데이트)
ECS_CLUSTER_NAME=
ECS_SERVICE_BACKEND=
ECS_SERVICE_FRONTEND=
